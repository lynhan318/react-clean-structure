const defaultState = {
  data: null,
  isAuthen: false,
  status: "",
};
export default (state = defaultState, { type, payload }) => {
  switch (type) {
    case "case":
      break;

    default:
      return state;
  }
};
